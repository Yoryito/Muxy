package com.muxy.app.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta "estanque": botánica, cálida y apagada. Nada de neón ni degradados fríos.
// Estos valores son la fuente de verdad — no redefinir colores en las pantallas.

// --- Verdes nenúfar (primario) ---
val MossDeep = Color(0xFF3F5738)
val Moss = Color(0xFF5E7F4F)
val MossLight = Color(0xFF87A876)
val MossPale = Color(0xFFD8E4CC)

// --- Aguas del estanque (secundario) ---
val PondDeep = Color(0xFF2C4F4D)
val Pond = Color(0xFF4F8B87)
val PondLight = Color(0xFF8CBDB8)
val PondPale = Color(0xFFD3E5E2)

// --- Flor de loto (acento, uso escaso: acciones activas, progreso) ---
val LotusDeep = Color(0xFFA85D5A)
/** Contenedor de acento para el modo oscuro: LotusDeep es un tono medio y usado
 *  como fondo sobre el estanque nocturno resulta chillón. */
val LotusNight = Color(0xFF4E2C2A)
val Lotus = Color(0xFFDE8B85)
val LotusLight = Color(0xFFF0B3AC)
val LotusPale = Color(0xFFF7DED9)

// --- Neutros cálidos ---
val Parchment = Color(0xFFFBF6EA)
val ParchmentRaised = Color(0xFFFFFCF4)
val Sand = Color(0xFFEADCC2)
val SandDeep = Color(0xFFCFBC9C)

// --- Estanque nocturno (modo oscuro; ajustado a mano, no una simple inversión) ---
val NightPond = Color(0xFF141E19)
val NightPondRaised = Color(0xFF1D2B24)
val NightPondHigh = Color(0xFF27382F)
val NightReed = Color(0xFF3A4F43)

// --- Tinta (texto: verde bosque profundo en vez de negro puro) ---
val InkForest = Color(0xFF1F2C1C)
val InkForestSoft = Color(0xFF4A5A45)
val InkCream = Color(0xFFEDF2E6)
val InkCreamSoft = Color(0xFFB4C2AC)

val ErrorClay = Color(0xFF9B4A3F)
val ErrorClayLight = Color(0xFFE8A79E)
